<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <ImageView
        android:id="@+id/edit_text_lefticon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/edit_text_rightpanel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="10dp" >

        <ImageView
            android:id="@+id/edit_text_clear"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:padding="5dp"
            android:src="@drawable/widget_clear"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/edit_text_eye"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginLeft="10dp"
            android:padding="5dp"
            android:src="@drawable/widget_eye_icon"
            android:visibility="gone" />
    </LinearLayout>

    <EditText
        android:id="@+id/edit_text_edt"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_marginLeft="10dp"
        android:layout_toLeftOf="@id/edit_text_rightpanel"
        android:layout_toRightOf="@id/edit_text_lefticon"
        android:background="@null"
        android:singleLine="true"
        android:textColor="@color/color_text_main"
        android:textColorHint="@color/color_text_third"
        android:textCursorDrawable="@null"
        android:textSize="15dp" />

</RelativeLayout>